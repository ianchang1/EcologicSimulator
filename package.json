{
  "name": "ecologic-simulator",
  "version": "1.0.0",
  "private": true,
  "description": "EcoLogic Simulator - Systems thinking simulation engine for environmental impact modeling",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
